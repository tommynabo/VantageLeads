# OpenAI API Key — Required for the AI Cognitive Layer (filtering, analysis, message generation).
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Neon Postgres Database URL — auto-injected when you link Neon Postgres from the Vercel dashboard.
# You do NOT need to set this manually in Vercel if you provision the database from the Vercel Storage tab.
DATABASE_URL="postgres://user:password@host/database?sslmode=require"
